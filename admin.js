/**
 * Custom Cursor Plugin - Admin JavaScript
 */

jQuery(document).ready(function($) {
    // This file is enqueued for future admin functionality
    // Currently, all JavaScript is inline in the settings page
    console.log('Custom Cursor Plugin admin scripts loaded');
});

